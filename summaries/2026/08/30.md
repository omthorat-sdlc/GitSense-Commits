# Activity Summary for 30/08/2026

## 00:17:10
The `/Users/omthorat/Milo/packages/frontend/src/lib/axios.config.ts` file, last modified on 29/08/2026 at 08:29:21, establishes the core Axios configuration for API calls within the Milo frontend application.

**File-specific updates and key functionalities include:**

*   **Axios Instance Configuration**: A single, global Axios instance (`api`) is created, specifically tailored for cross-origin requests between the frontend (port 3001) and the FastAPI backend (port 8000). It explicitly sets `withCredentials: true` to ensure httpOnly session cookies are sent with every request, crucial for authentication.
*   **Dynamic Base URL and Timeout**: The `API_BASE_URL` is dynamically resolved, prioritizing `process.env.API_URL` (for server-side rendering/internal access), then `process.env.NEXT_PUBLIC_API_URL` (for browser-side access), with a fallback to `http://127.0.0.1:8000`. It also handles potential trailing slashes in the URL to prevent issues with FastAPI. Similarly, `API_TIMEOUT_MS` is configurable via `process.env.NEXT_PUBLIC_API_TIMEOUT_MS` or defaults to 15 seconds.
*   **Custom Request Options**: The Axios `AxiosRequestConfig` is augmented with `MiloRequestOptions`. These include:
    *   `requireAuth?: boolean`: A flag to indicate if an endpoint requires an authenticated session. This is vital for the error interceptor to distinguish between a truly expired session (which triggers a redirect to `/login`) and other 401 errors (e.g., incorrect credentials on a login endpoint).
    *   `skipErrorNormalisation?: boolean`: An escape hatch for callers who need to receive the raw `AxiosError` instead of the custom `ApiError`.
*   **Robust Error Handling**: A central `ApiError` class is introduced to normalize all API-related failures into a consistent structure. This class provides `code`, `msg`, `status`, `details`, `retryAfterSeconds` (parsed from `Retry-After` header for 429 errors), and an `isSessionExpired` getter. The `toApiError` function converts various `AxiosError` types (e.g., request cancelled, timeout, network error, server errors) into this standardized `ApiError`, making error handling predictable throughout the application. It specifically translates a 401 error on an endpoint marked `requireAuth` into a `session_expired` error.