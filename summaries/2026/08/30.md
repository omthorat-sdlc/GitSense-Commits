# Activity Summary for 30/08/2026

## 00:17:10
The `/Users/omthorat/Milo/packages/frontend/src/lib/axios.config.ts` file, last modified on 29/08/2026 at 08:29:21, establishes the core Axios configuration for API calls within the Milo frontend application.

**File-specific updates and key functionalities include:**

*   **Axios Instance Configuration**: A single, global Axios instance (`api`) is created, specifically tailored for cross-origin requests between the frontend (port 3001) and the FastAPI backend (port 8000). It explicitly sets `withCredentials: true` to ensure httpOnly session cookies are sent with every request, crucial for authentication.
*   **Dynamic Base URL and Timeout**: The `API_BASE_URL` is dynamically resolved, prioritizing `process.env.API_URL` (for server-side rendering/internal access), then `process.env.NEXT_PUBLIC_API_URL` (for browser-side access), with a fallback to `http://127.0.0.1:8000`. It also handles potential trailing slashes in the URL to prevent issues with FastAPI. Similarly, `API_TIMEOUT_MS` is configurable via `process.env.NEXT_PUBLIC_API_TIMEOUT_MS` or defaults to 15 seconds.
*   **Custom Request Options**: The Axios `AxiosRequestConfig` is augmented with `MiloRequestOptions`. These include:
    *   `requireAuth?: boolean`: A flag to indicate if an endpoint requires an authenticated session. This is vital for the error interceptor to distinguish between a truly expired session (which triggers a redirect to `/login`) and other 401 errors (e.g., incorrect credentials on a login endpoint).
    *   `skipErrorNormalisation?: boolean`: An escape hatch for callers who need to receive the raw `AxiosError` instead of the custom `ApiError`.
*   **Robust Error Handling**: A central `ApiError` class is introduced to normalize all API-related failures into a consistent structure. This class provides `code`, `msg`, `status`, `details`, `retryAfterSeconds` (parsed from `Retry-After` header for 429 errors), and an `isSessionExpired` getter. The `toApiError` function converts various `AxiosError` types (e.g., request cancelled, timeout, network error, server errors) into this standardized `ApiError`, making error handling predictable throughout the application. It specifically translates a 401 error on an endpoint marked `requireAuth` into a `session_expired` error.

## 01:17:13
The file `/Users/omthorat/Milo/packages/frontend/src/lib/axios.config.ts`, last modified on 29/08/2026 at 08:29:21, establishes a centralized Axios instance for all Milo API calls.

Key updates include:

*   **Configuration for cross-origin requests and authentication:** The `api` instance is explicitly configured with `withCredentials: true` to ensure that `httpOnly` session cookies set by the backend are sent with cross-origin requests, which is crucial for authentication.
*   **Dynamic API Base URL and Timeout Resolution:** Functions `resolveBaseUrl()` and `resolveTimeout()` determine the API endpoint and request timeout. These prioritize server-side environment variables (`process.env.API_URL`) over public ones (`process.env.NEXT_PUBLIC_API_URL`, `process.env.NEXT_PUBLIC_API_TIMEOUT_MS`), with default fallbacks. A utility is included to remove trailing slashes from the base URL.
*   **Custom Request Options and Types:**
    *   `MiloRequestOptions` is introduced to extend `AxiosRequestConfig` with custom, per-request options like `requireAuth` (to flag endpoints that need a signed-in session for specific error handling) and `skipErrorNormalisation` (for callers needing the raw `AxiosError`).
    *   `ApiErrorPayload` defines a standard structure for error responses from the backend (containing `code`, `msg`, `details`).
*   **Normalized Error Handling:**
    *   A custom `ApiError` class is implemented to provide a consistent error interface across the frontend. This class encapsulates `code`, `msg`, `status`, `details`, and `retryAfterSeconds` (parsed from `Retry-After` header for 429 errors). It also includes an `isSessionExpired` getter.
    *   The `toApiError()` function is responsible for intercepting `AxiosError` instances and converting them into the `ApiError` format. It handles various scenarios: request cancellation, timeouts, network errors (e.g., backend down, CORS issues), session expiration (specifically for 401 errors on `requireAuth` endpoints), and parsing structured error payloads from the backend, otherwise defaulting to a generic `internal_error`.
*   **Axios Instance Initialization:** The `api` constant creates the Axios instance with the resolved `baseURL`, `timeout`, mandatory `withCredentials: true`, and default `Content-Type` and `Accept` headers set to `application/json`.

## 02:36:05
The file `/Users/omthorat/Milo/packages/frontend/src/lib/axios.config.ts`, last updated on 29/08/2026 at 08:29:21, defines the core Axios instance and associated utilities for all API calls in the Milo frontend application.

Key functionalities include:

*   **Axios Configuration:** It establishes a single Axios instance (`api`) with essential settings. The base URL (`API_BASE_URL`) is dynamically resolved, prioritizing `process.env.API_URL` (for server-side rendering) or `process.env.NEXT_PUBLIC_API_URL` (for client-side), defaulting to `http://127.0.0.1:8000`. A default timeout of 15 seconds (`API_TIMEOUT_MS`) is set, configurable via `process.env.NEXT_PUBLIC_API_TIMEOUT_MS`. Crucially, `withCredentials: true` is enabled to ensure the browser sends httpOnly session cookies for cross-origin requests, which is vital for authentication.
*   **Custom Request Options:** The file introduces `MiloRequestOptions` to extend standard Axios request configurations. These options include `requireAuth` (to flag endpoints needing an authenticated session, aiding in error handling) and `skipErrorNormalisation` (to bypass custom error processing for specific cases). Axios's `AxiosRequestConfig` is augmented to include these custom options.
*   **Standardized Error Handling:** A central `ApiError` class is defined to normalize all API-related failures into a consistent format. This class provides properties like `code`, `msg`, `status`, `details`, and `retryAfterSeconds` (for 429 responses). A `isSessionExpired` getter simplifies checking for session expiry errors. The `toApiError` function serves as a robust error interceptor, transforming raw `AxiosError` objects into `ApiError` instances. It handles various scenarios:
    *   Request cancellation (`request_cancelled`).
    *   Timeouts (`timeout`).
    *   Network issues (backend down, DNS failure, CORS preflight) resulting in `network_error`.
    *   **Special handling for 401 Unauthorized:** If an endpoint marked with `requireAuth` returns a 401, it's specifically mapped to a `session_expired` error, indicating a need for user re-authentication. Otherwise, a 401 uses the backend's provided error code.
    *   Parses backend-defined `ApiErrorPayload` for detailed error information.
    *   Provides a generic `internal_error` fallback for unhandled error shapes.

**Patterns and Recurring Elements:**

*   **Environment Variable Dependency:** The configuration heavily relies on `process.env` variables (e.g., `API_URL`, `NEXT_PUBLIC_API_URL`, `NEXT_PUBLIC_API_TIMEOUT_MS`) for dynamic setup, distinguishing between server-only and public variables.
*   **Cross-Origin Authentication:** The explicit setting of `withCredentials: true` highlights a consistent pattern for handling cookie-based, cross-origin authentication with the FastAPI backend.
*   **Error Normalization:** A strong emphasis on transforming diverse API errors into a single, predictable `ApiError` shape is a recurring theme, ensuring consistent error reporting across the frontend application.
*   **Module Augmentation:** The use of `declare module "axios"` demonstrates a pattern for extending third-party library types to integrate custom options seamlessly.